# 2026-09-17

## rank 项目 App 模式适配（dev 分支，改动文件：Rank.vue / app-mode.less）
- `Rank.vue`：meta viewport 注入改为复用已有标签（querySelector，避免重复不生效）；导入切换到项目本地 `src/utils/env.js` 的 `syncAppEnv` / `applyAppEnv`，小程序/手机 UA 判定内联在组件 computed 中（本地 env.js 无 isMiniProgram 导出）。
- `app-mode.less`（`.v-app` 作用域）：
  - `.m-race-tab` 改 flex 单行横向滚动（overflow-x auto、隐藏滚动条）；`.u-item` 文字渐变用 `background-clip: text + color: transparent`（CSS 文本无 fill 属性，勿用）；`&::after`（选中指示条）嵌套在 `&.active` 内。
  - Info 页（`.m-rank-content .m-rank-info` / `.w-time-line-box`）按设计图适配：去 #264259 底色与 8px 金色上边框，分区标题改为白字+4px 金色左竖条，正文 #e6edf3；按钮 `.u-btn` 保持 bg 图（锚点为空、文字在图里），用 `aspect-ratio: 480/95` 缩放（join 全宽 max 480px，weal 200px）；时间线整体缩放（高 160px、字号缩小）。
- `time_line.vue`：已删除「当前进度」标签（JS 注入的 progressDiv）和左右切换箭头（模板/scroll 方法/currentIndex、totalDay、arrow 数据及 .u-arr/.progress 样式、__imgPath 导入均移除）；setLiPosition 简化为仅首次按 nearest 居中。此改动全局生效（PC 端箭头原本就被 clip 隐藏）。
- app 时间线交错排版（app-mode.less `.v-app .w-time-line-box`）：li 高 161px、top -80px（横线在 li 垂直中心 top:80px）；菱形改为 `u-time::after` 绝对定位置于横线中心（translate(-50%,-50%) rotate(45deg) 12px）；`li:nth-child(even)` 用 `order:-1` 让标签在上、日期在下（奇数节点日期在上、标签在下），even 的 label `::before` 连接茎隐藏。
- 注意：race_tab.less 的 tab 基础样式是 167x63 图片背景 + `.dbi` 行内块，app 模式全部覆盖为纯文字样式。用户会直接手改这些 less 文件，编辑前必须重读文件（多次出现行号偏移/内容已变）。
