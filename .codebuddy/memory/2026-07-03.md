# 2026-07-03

## Dps2.vue 合并 item.vue 组件内容
- 将 `src/views/Superstar/dps/item.vue` 的模板、方法和样式全部合并到 `src/views/Superstar/Dps2.vue` 中
- 模板：将原来的占位 div（`m-box`, `m-box-none`）替换为 item.vue 的完整排名展示模板，包含团队信息卡片、排行列表、心法图标等
- 脚本：新增 import（showTime, getThumbnail, colorData, xfmap），新增方法（jclLink, battleLink, teamLogo, showMount, showMountSvg, showMountColor, getBarWidth, showTime, showTC）
- 样式：Dps2.vue 新增 `<style>` 块引入 `dps_v2.less`
- 修复 `_.first(Object.keys(this.bossList))` 为 `Object.keys(this.bossList)[0] || ""`
- 移除未使用的 import（getDps, getEvents）
- 移除了空的 changeType 方法，添加 active watch 处理 tab 切换

## Dps_v22.vue 合并 item.vue 组件内容
- 将 item.vue 模板合并入 `src/views/Superstar/Dps_v22.vue`，替换 4 个 `<item>` 标签为内联模板
- 保留 `xfItem` 和 `die` 作为独立组件
- 新增 3 组独立 data：clearSpeed（通关速度）、team（团队统计）、player（个人统计），各自有 active/item/activeId/data
- 新增 3 个 init 方法：initClearSpeed、initTeam、initPlayer，在 dataInit 中调用
- 新增 teamActive/playerActive watch 响应 tab 切换
- 移除 item 组件 import 和 components 注册
- 修复 `_.first` 为原生写法

## Dps_v22.vue 合并 xfItem.vue 组件内容
- 将 `src/views/Superstar/dps/xfItem.vue` 模板内联替换 `<xfItem>` 标签
- 新增 data：xfActive（默认0）、xfData（心法排行数据）
- 新增 watch：xfActive 切换时调用 initXf，sortByForce 变化时自动初始化
- 新增方法：initXf（处理 forceId → xfId 映射并排序）、showXfMountColor（心法颜色）、getXfBarWidth（750px 柱状图宽度）
- 移除 xfItem 组件 import 和 components 注册
- 注意：xfItem 的 showMountColor 和 getBarWidth 签名与 item 不同，使用独立命名（showXfMountColor、getXfBarWidth）避免冲突

## Dps_v22.vue 按 Dps2 模式重构为 dataList 循环
- 参照 Dps2.vue 的 `dataList` 模式，将 Dps_v22 中 4 个独立模板区块整合为 `v-for="(item, index) in dataList"` 循环结构
- `dataList` 定义在 data 中（非 computed），包含 title/type/bg/data/active/activeId/curItem/key
  - key: 'clearSpeed' | 'team' | 'xf' | 'player'
  - type: false（无tab）| true（团队tab）| 'player'（个人tab）| 'none'（心法）
  - bg: 'left'（绿色左）| 'right'（白色右）| 'none'（心法专用）
- 模板中根据 item.bg 和 item.type 条件渲染不同布局：
  - bg === 'none' → 心法平均专用布局（u-xf + u-team-rank_itemNo）
  - 其他 → 团队/个人通用布局，通过 item.type === 'player' 区分个人卡片
- 脚本重构：
  - 移除所有独立 data（clearSpeed*, team*, xf*, player*），统一为 dataList 数组
  - 移除独立 init 方法（initClearSpeed/initTeam/initXf/initPlayer），合并为 initItem(listItem) 根据 key 分发
  - 新增 onItemClick(listItem, row) 统一处理排行项点击
  - 修改 showMountColor(index, row, listItem) → 根据 listItem.type === 'player' 判断心法颜色
  - 修改 getBarWidth(row, i, listItem) → 根据 listItem.type 决定取值字段
  - 新增 dataList deep watch → active 变化时自动重新 initItem
  - 新增 imgUrl: __cdn + "design/event/superstar/" 用于 flower 图片
